{
  "buildCommand": "cd frontend && npm install && npm run build && npx xcopy /E /I /Y dist ..\\\\dist 2>nul || cp -r dist ../dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}